//Instalar o módulo prompt-sync (npm install prompt-sync);
const prompt = require ('prompt-sync') ();
//Solicitar um numero ao usuário;
numero = prompt ("insira um numero inteiro: ");
//faço drecréscimo do numero do usuario.
for (i = numero; i >= numero; i++) {
    console.log("Numero: ", i);
}